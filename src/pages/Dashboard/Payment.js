import React from 'react';

const Payment = ({_id,name,picture,description,price,quantity}) => {
    return (
        <div>
            <h1>payment this amount</h1>
        </div>
    );
};

export default Payment;